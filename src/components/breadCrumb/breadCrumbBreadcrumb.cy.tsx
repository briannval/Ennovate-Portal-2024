import React from "react";
import Breadcrumb from "./breadCrumb";

describe("<Breadcrumb />", () => {
  it("renders", () => {
    // see: https://on.cypress.io/mounting-react
    // cy.mount(<Breadcrumb />)
  });
});
