import React from "react";
import BlogSection from "./blogSection";

describe("<BlogSection />", () => {
  it("renders", () => {
    // see: https://on.cypress.io/mounting-react
    // cy.mount(<BlogSection featured={false} />);
  });
});
