import React from "react";
import TeamMemberGrid from "./teamMemberGrid";

describe("<TeamMemberGrid />", () => {
  it("renders", () => {
    // see: https://on.cypress.io/mounting-react
    // cy.mount(<TeamMemberGrid />)
  });
});
